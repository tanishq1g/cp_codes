// cmath's abs example
#include <iostream>     // std::cout
#include <cmath>        // std::abs

int main ()
{

// abs
  cout << "abs (3.1416) = " << std::abs (3.1416) << '\n';
  cout << "abs (-10.6)  = " << std::abs (-10.6) << '\n';

// sqrt
    double sqrt (double x);
    param = 1024.0;
    result = sqrt (param);

// pow
    printf ("7 ^ 3 = %f\n", pow (7.0, 3.0) );
    printf ("4.73 ^ 12 = %f\n", pow (4.73, 12.0) );
    printf ("32.01 ^ 1.54 = %f\n", pow (32.01, 1.54) )

//max, min, fmax, fmin
    max, min, fmax, fmin -- two args, returns 1 output

// floor ceil
    printf ( "floor of 2.3 is %.1lf\n", floor (2.3) ); 2
    printf ( "floor of 3.8 is %.1lf\n", floor (3.8) ); 3
    printf ( "floor of -2.3 is %.1lf\n", floor (-2.3) ); -3
    printf ( "floor of -3.8 is %.1lf\n", floor (-3.8) ); -4
    printf ( "ceil of 2.3 is %.1f\n", ceil(2.3) ); 3
    printf ( "ceil of 3.8 is %.1f\n", ceil(3.8) ); 4
    printf ( "ceil of -2.3 is %.1f\n", ceil(-2.3) ); -2
    printf ( "ceil of -3.8 is %.1f\n", ceil(-3.8) ); -3

  return 0;
}
